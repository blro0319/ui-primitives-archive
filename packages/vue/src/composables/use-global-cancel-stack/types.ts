export interface UseGlobalCancelStackOptions {
  /**
   * @default
   * ```ts
   * true
   * ```
   */
  escape?: boolean;
  /**
   * @default
   * ```ts
   * true
   * ```
   */
  history?: boolean;
}
